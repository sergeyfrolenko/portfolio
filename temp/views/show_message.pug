html
   head
      title Person
   body
      if(type == "error")
         h3(style = "color:red") #{message}
      else
         h3 New person, 
         | name: #{user.name}, 
         | age: #{user.age} added!
